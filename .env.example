# ==========================================================
# TradingInfoTool — Vorlage für Umgebungsvariablen
# ----------------------------------------------------------
# So benutzen:
#   1. Diese Datei kopieren und in ".env" umbenennen
#         Windows (PowerShell):  Copy-Item .env.example .env
#   2. Die Platzhalter unten durch deine echten Werte ersetzen
#   3. Die .env NIEMALS committen (ist bereits in .gitignore)
#
# WICHTIG: Diese .env.example enthält absichtlich NUR Platzhalter.
#          Echte Tokens/Keys gehören ausschliesslich in die .env.
# ==========================================================

# GitHub Personal Access Token
# Erstellen/verwalten: https://github.com/settings/tokens
# Tipp: minimale Rechte (scopes) + Ablaufdatum (expiration) setzen.
GITHUB_TOKEN=dein_github_token_hier

# Claude API Key (Anthropic)
# Erstellen/verwalten: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=dein_anthropic_api_key_hier

# CoinGecko Demo API Key (kostenlos, kein Kreditkarte noetig)
# Erstellen: https://www.coingecko.com/en/api/pricing -> "Create Free Account" ->
#            Developer's Dashboard -> API Key generieren
# Ohne Key: 30 Req/Min (anonym, in der Praxis unzuverlaessig - laengere Sperren moeglich)
# Mit Demo-Key: 100 Req/Min, stabiler. App funktioniert auch ohne Key (Fallback),
# aber ein Key wird empfohlen.
COINGECKO_API_KEY=dein_coingecko_key_hier

# Groq API Key (kostenlos, kein Kreditkarte noetig, kein KYC)
# Erstellen: https://console.groq.com -> Anmeldung per Google/GitHub/E-Mail (<60 Sek.) ->
#            "API Keys" -> "Create API Key"
# Primaere KI-Analyse-Ebene ab Phase 3 (Llama 3.3 70B via Groq LPU-Hardware).
# Kein Kraken-Key noetig (oeffentliche Endpunkte, siehe Basisinfos/Spezifikation.md Kap. 8).
GROQ_API_KEY=dein_groq_key_hier

# Mistral AI API Key (kostenlos, "Free Mode"/"Experiment"-Tier, kein Kreditkarte
# noetig) - 2026-07-17, zweite Fallback-Stufe, ersetzt Cerebras' bisherige Rolle
# vollstaendig (Cerebras wurde 2026-07-17 komplett aus der aktiven Fallback-Kette
# entfernt, siehe Memory project_cerebras_free_tier_aenderung_2026-08-17.md).
# Echt im eigenen Kontingent-Dashboard verifiziert: mistral-small-2506 -
# 2.250.000 Tokens/Min, 5 Req/Sek (~300/Min) - mit Abstand groesszuegigste
# Kapazitaet aller LLM-Anbieter. Vertraglich guenstiger als Gemini: keine
# EWR/CH/UK-Sonderklausel, keine Warnung vor vertraulichen/Finanzdaten,
# Trainings-Nutzung im Free-Tier abwaehlbar.
# Erstellen: https://console.mistral.ai -> Account anlegen (kein Kreditkarte
# noetig fuer "Free Mode") -> "API Keys" -> "Create new key".
# Bei der Erstellung: Connector-Zugriffsumfang auf "Shared connectors only"
# (Standard) belassen - wir nutzen Mistrals "Connectors"-Feature (RAG-/Tool-
# Anbindungen) gar nicht, "Private and shared connectors" waere unnoetig
# weitreichender Zugriff.
MISTRAL_API_KEY=dein_mistral_key_hier

# OBSOLET FUER DIE PRODUKTION (2026-07-17) - Cerebras wurde vollstaendig aus der
# aktiven Fallback-Kette entfernt (Mistral oben hat die Rolle uebernommen, siehe
# Memory project_cerebras_free_tier_aenderung_2026-08-17.md). Zeile bleibt bewusst
# unveraendert als Referenz stehen (Nutzer-Entscheidung), wird von main.py NICHT
# mehr gelesen - kein Code-Pfad haengt noch davon ab.
CEREBRAS_API_KEY=dein_cerebras_key_hier

# [OPTIONAL, AKTUELL NICHT VERDRAHTET - kein main.py-Import/Client-Aufbau]
# xAI (Grok) API Key (2026-07-14, NICHT weiterverfolgt - siehe Memory
# project_xai_grok_option.md: $150/Monat-Datenteilen-Programm laut xAI-Support
# offiziell eingestellt, nur noch bezahltes SuperGrok-Pro-Abo verfuegbar,
# widerspricht der "nur kostenfrei"-Regel. $5 Test-Guthaben vorhanden, nur als
# seltene Notfall-Absicherung gedacht, keine aktive Integration geplant).
# Bewusst NICHT geloescht: $5-Guthaben bleibt bestehen, u.U. spaeter im Projekt
# nutzbar (z.B. falls sich die Kostenfrei-Policy oder xAIs Konditionen aendern).
XAI_API_KEY=dein_xai_key_hier

# Google Gemini API Key (2026-07-14, kostenlos, kein Kreditkarte noetig) -
# bewusst die LETZTE, seltenste Fallback-Stufe (2026-07-17 nach Mistral-
# Einfuehrung neu eingeordnet, siehe Memory
# project_cerebras_free_tier_aenderung_2026-08-17.md): echt verifizierte
# Kapazitaet (gemini-3.1-flash-lite - 15 Req/Min, 250K Tokens/Min) reicht zwar
# locker aus, aber Gemini hat von allen Anbietern die ungueenstigsten
# Vertragsbedingungen (EWR/CH/UK-Sonderklausel + explizite Warnung vor
# vertraulichen/Finanzdaten + NICHT abwaehlbare Trainings-Nutzung, anders als
# Groq/Mistral) - soll deshalb moeglichst selten drankommen.
# Erstellen: https://aistudio.google.com -> Anmeldung -> "Get API key" ->
# "Create API key".
GEMINI_API_KEY=dein_gemini_key_hier

# FRED API Key (St. Louis Fed, kostenlos, kein Kreditkarte noetig)
# Erstellen: https://fredaccount.stlouisfed.org/apikeys (Key wird sofort vergeben)
# Fed/EZB/M2/CPI/ISM-Ersatz(Philly-Fed)/BoJ/BoK-Leitzinsen fuer Makro-Kontext (R-5.1/-5.2),
# siehe api/macro.py + Basisinfos/Spezifikation.md Kap. 8. Kein Key fuer PBoC noetig
# (Eastmoney-Endpunkt, oeffentlicher Token bereits im Code).
FRED_API_KEY=dein_fred_key_hier

# BGeometrics / bitcoin-data.com API Key (kostenlos, Registrierung z.B. per Google)
# Erstellen: https://portal.bgeometrics.com/signup -> Login/Profile -> API Key
# On-Chain-Metriken (MVRV-Z-Score, NUPL, Realized Price, SOPR) - Free-Tier: 10 Req/Std,
# 15 Req/Tag, daher sparsam abrufen (siehe api/macro.py). Kap. 8/16 der Spezifikation.
BGEOMETRICS_API_KEY=dein_bgeometrics_key_hier

# Bank of Korea ECOS API Key (kostenlos, aber Registrierung mit Identitaetspruefung
# noetig - kann fuer Nicht-Koreaner/ohne koreanische Telefonnummer ein Hindernis sein)
# Erstellen: https://ecos.bok.or.kr/#/ -> Registrierung -> "OPEN API 인증키 신청"
# Key wird laut Doku "meist innerhalb 1 Tag" nutzbar, nicht immer sofort wie bei FRED.
# Korea-M2 (5. Notenbank neben Fed/EZB/BoJ/PBoC) fuer die globale Liquiditaetssicht,
# siehe api/macro.py + Basisinfos/Spezifikation.md Kap. 8.
ECOS_API_KEY=dein_ecos_key_hier

# Bitpanda API Key (nur lesend genutzt - Bestands-/Cash-Reserve-Abgleich, siehe
# importer/bitpanda_sync.py). Erstellen/verwalten: bitpanda.com -> Account -> API Keys.
# Scope "Balance" genuegt; ein Key mit "Trading, Transaction, Balance" funktioniert
# ebenso, da die App ausschliesslich GET-Aufrufe macht (P-7, kein Order-/Auszahlungs-
# zugriff ueber Bitpanda API-Keys ueberhaupt moeglich - alle Scopes sind laut Bitpanda-
# Doku rein lesend). Ohne Key bleibt die manuelle Excel-Import/Export- und Fiat-Cash-
# Eingabe (Portfolio-Tab) voll nutzbar (P-8: Kernfunktion braucht den Key nicht).
BITPANDA_API_KEY=dein_bitpanda_key_hier

# Remote-Steuer-Seite (2026-07-11, siehe remote/server.py + Basisinfos/
# Tailscale-Setup-Anleitung.md + Regelwerksmanual.md Kap. 12/13). Erreichbar nur
# innerhalb des Tailscale-VPN-Netzes, dieser Token ist eine ZWEITE Schutzschicht
# obendrauf. Ohne diesen Wert bleibt die Steuer-Seite komplett deaktiviert - kein
# lauschender Port, kein Risiko (P-8). Erzeugen mit:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
REMOTE_ACCESS_TOKEN=dein_generierten_token_hier

# E-Mail-Benachrichtigung (U-8, 2026-07-12, siehe Basisinfos/Regelwerksmanual.md
# Kap. 12). BEWUSST ein separater, eigens angelegter "Robot"-Google-Account, NICHT
# dein Hauptaccount - das App-Passwort ist ein dauerhafter, programmatischer
# Zugriffsschluessel zur gesamten Mailbox, der auf dem 24/7-Notebook liegt.
# Einrichtung: 1) neuen Google-Account anlegen, 2) dort 2-Schritt-Verifizierung
# aktivieren (Kontoeinstellungen -> Sicherheit) - Voraussetzung fuer App-Passwoerter,
# 3) Kontoeinstellungen -> Sicherheit -> "App-Passwoerter" -> beliebigen Namen
# vergeben (z.B. "TradingInfoTool") -> Erzeugen, das 16-stellige Ergebnis ist der
# folgende Wert (ohne Leerzeichen). Ohne diese beiden Werte bleibt die
# E-Mail-Benachrichtigung komplett deaktiviert, kein Fehler (P-8).
GMAIL_ABSENDER_ADRESSE=dein_robot_account@gmail.com
GMAIL_APP_PASSWORT=dein_16_stelliges_app_passwort_hier
